# 작업 안내

가장 가까운 `AGENTS.md`를 먼저 따라요. 루트 문서는 공통 규칙과 탐색 경로만 제공하고, 세부 규칙은 각 워크스페이스 문서에 둬요. 저장소를 탐색하기 전 [`docs/README.md`](docs/README.md)의 경로 표를 확인해요.

이 저장소는 스펙을 먼저 확정하고 구현하는 방식(SDD)으로 작업해요. 코드를 만지기 전에 [`specs/README.md`](specs/README.md)에서 해당 스펙의 상태를 확인해요.

## 핵심 원칙

- 스펙을 먼저 갱신하고 구현해요. 스펙에 없는 동작은 구현하지 않고, 구현이 스펙과 어긋나면 스펙을 고치거나 구현을 되돌려요.
- 세 단계(발견 · 조립 · 배포)는 병렬 기능이 아니라 직렬 파이프예요. 단계 경계를 넘는 데이터는 접합부 스펙이 정의한 타입만 사용해요.
- `main`에 직접 푸시하지 않아요. `<type>/<description>` 브랜치와 PR을 사용해요.
- 커밋은 Conventional Commits 형식을 사용하고 scope를 반드시 적어요. 허용 scope는 `web`, `ui`, `graph`, `canvas`, `serve`, `catalog`, `specs`, `root`예요.
- 패키지 매니저는 pnpm이에요. `npm install`, `yarn add`로 의존성을 추가하지 않아요.
- 코드와 코드 주석, 커밋 메시지, PR 설명은 영어로 작성해요. 문서는 한국어 해요체로 작성해요. 한국어 문장에서 em dash(U+2014)를 사용하지 않아요.
- 문서는 현재 계약과 결정만 설명해요. 변경 이력, 과거 상태와 완료된 계획은 Git 기록에 맡겨요.
- 사용자가 없는 프로젝트이므로 하위 호환 계층, 임시 리디렉션, 사용하지 않는 환경 변수와 문서를 남기지 않아요.
- 생성 파일(`**/__generated__/**`, 스키마 산출물)은 직접 편집하지 않고 생성 명령으로 갱신해요.
- 설명용 주석은 지워요. 외부 계약이나 이해하기 어려운 불변 조건만 JSDoc으로 남겨요.
- 공공데이터 API 키와 모델 키를 커밋하지 않아요. 이름만 `.env.example`에 남기고 값은 로컬과 배포 비밀 값으로 관리해요.

## 저장소 지도

| 경로                       | 역할                                   | 추가 지침                                                            |
| -------------------------- | -------------------------------------- | -------------------------------------------------------------------- |
| `specs`                    | 구현 전에 합의하는 동작과 계약         | [`specs/README.md`](specs/README.md)                                 |
| `docs`                     | 탐색 경로와 품질 계약                  | [`docs/README.md`](docs/README.md)                                   |
| `research`                 | 현재 결정에 필요한 외부 근거           | [`research/README.md`](research/README.md)                           |
| `apps/web`                 | Next.js 웹 앱                          | [`apps/web/AGENTS.md`](apps/web/AGENTS.md)                           |
| `packages/design-system`   | 토큰, 레시피, 공용 UI 컴포넌트         | [`packages/design-system/AGENTS.md`](packages/design-system/AGENTS.md) |
| `apps/*`                   | 배포 단위 (Next.js 웹, 배포 런타임 등) | 각 앱의 `AGENTS.md`                                                  |
| `packages/*`               | 앱이 공유하는 도메인 로직, UI, 설정    | 각 패키지의 `AGENTS.md`                                              |

워크스페이스를 새로 만들면 그 폴더에 `AGENTS.md`를 함께 만들고 이 표에 한 줄을 추가해요.

## 기술 스택

| 영역           | 선택                    | 규칙                                                                 |
| -------------- | ----------------------- | -------------------------------------------------------------------- |
| 앱 프레임워크  | Next.js App Router      | 서버 컴포넌트를 기본으로 두고 필요한 경계에서만 `"use client"`를 써요 |
| 언어           | TypeScript strict       | `any`와 근거 없는 단언을 쓰지 않아요                                 |
| 스타일         | Tailwind CSS v4         | CSS의 `@theme`로 토큰을 정의하고 `tailwind.config` 파일을 만들지 않아요 |
| React 유틸     | react-simplikit         | 훅과 유틸 컴포넌트를 직접 만들기 전에 여기에 있는지 먼저 확인해요    |
| 데이터 유틸    | es-toolkit              | lodash와 자체 배열·객체 헬퍼를 추가하지 않아요                       |
| 분기           | ts-pattern              | 판별 유니온과 다중 조건 분기는 `match`로 표현해요                    |
| 패키지 매니저  | pnpm workspace          | 워크스페이스 간 참조는 `workspace:*`를 써요                          |

스택을 바꾸려면 `research/external`에 근거를 남기고 이 표를 함께 갱신해요.

## 구현 규칙

- 파일과 디렉터리는 kebab-case, React 컴포넌트는 PascalCase, 함수와 변수는 camelCase를 사용해요.
- 도메인 타입과 순수 로직은 `packages`에 두고 `apps`는 조립과 표현만 담당해요. 앱끼리 직접 import하지 않아요.
- 파이프라인 경계 데이터(제안, 워크플로, 배포 단위)는 스키마로 검증한 뒤 통과시켜요. 검증되지 않은 외부 응답을 도메인 타입으로 단언하지 않아요.
- AI는 선언적 스펙을 만들고, 실행은 결정적인 코드가 담당해요. 모델이 만든 코드를 실행하지 않아요.
- 실패는 조용히 넘기지 않아요. 행 수 급감, 지역 매칭률 급락, 타입·단위 충돌, 결측률 급증은 사용자에게 보이는 정지 신호로 다뤄요.
- 공용 UI에 같은 역할의 컴포넌트가 있으면 앱에서 다시 만들지 않아요.
- 외부 사례는 1차 자료로 확인하고 `research/`에 채택 여부, 출처 제약, 로컬 근거를 기록해요.
- 관련 없는 변경을 섞지 않고, 사용자 변경이 있는 작업 트리에서는 해당 파일을 보존해요.
- 단위 테스트는 소스 옆 `*.test.ts`, 타입 계약은 `*.type-test.ts`에 둬요.

## 검증

| 목적                | 명령               |
| ------------------- | ------------------ |
| 정적 검사           | `pnpm lint`        |
| 타입 검사           | `pnpm typecheck`   |
| 단위 테스트         | `pnpm test`        |
| 푸시 전 전체 확인   | `pnpm verify`      |

접합부 스키마, 지역코드 정규화, 조인 결과 판정과 배포 계약은 반드시 테스트해요. 단순 렌더링, CSS 클래스 목록, 프레임워크 기본 동작은 테스트하지 않아요. 자세한 기준은 [`docs/QUALITY.md`](docs/QUALITY.md)에 있어요.

## GitHub 작업

- 변경 파일만 명시적으로 stage하고 생성 파일은 소스 계약이 바뀐 경우에만 포함해요.
- PR 제목은 영어 Conventional Commit 형식과 허용 scope를 사용해요.
- 동작을 바꾸는 PR은 근거가 되는 스펙 경로를 설명에 적어요.
- 리뷰 의견을 처리하면 답변을 먼저 남기고 해당 스레드를 resolve해요.
- 모든 필수 CI가 통과한 뒤에만 병합해요.
- 병합은 squash merge만 사용해요. merge commit과 rebase는 저장소 설정에서 막혀 있어요.
- squash 커밋 제목은 PR 제목을 쓰고 본문은 브랜치의 커밋 메시지를 모아요. 그래서 PR 제목과 커밋 메시지를 영어로 적어요.
- 병합하면 브랜치를 삭제해요. 저장소 설정이 자동으로 지워줘요.
