import { StagePlaceholder } from "../_components/stage-placeholder";

export default function ServePage() {
  return (
    <StagePlaceholder
      handoff="API · MCP"
      stage="배포"
      summary="워크플로를 고정 엔드포인트로 노출해요. 같은 입력이면 같은 결과를 돌려줘요."
      title="배포"
    />
  );
}
